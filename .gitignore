# Vercel
.vercel/

# Deps (немає, але про всяк)
node_modules/

# Локальне
.DS_Store
*.log

# Local env (Vercel functions секрети — тільки у dashboard)
.env
.env.local
.env.*.local
.secrets/

# Audio archives — kept на диску, не у repo (bloat)
public/audio/_archive-*/
